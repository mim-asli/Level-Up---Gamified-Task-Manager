/**
 * This file's purpose is to import all command definition files.
 * This ensures that the `registerCommand` function within each file is called
 * and all commands are added to the registry upon application startup.
 */
import './paletteCommands.js';
import './navigationCommands.js';
import './systemCommands.js';
